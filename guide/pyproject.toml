[project]
name = "guide"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[project.scripts]
guide = "main:main"

[tool.setuptools]
py-modules = ["main"]
