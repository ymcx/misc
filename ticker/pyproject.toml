[project]
name = "ticker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "yfinance>=1.3.0",
]

[project.scripts]
ticker = "main:main"

[tool.setuptools]
py-modules = ["main"]
